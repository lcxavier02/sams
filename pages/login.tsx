import LogIn from '@/components/LogIn'
import React from 'react'

/**
 * Login Page Component
 *
 * This is a simple wrapper component for the `LogIn` component. It serves as the login page
 * for the application by rendering the `LogIn` component.
 *
 * @component
 *
 * @example
 * return (
 *   <Login />
 * )
 */
function Login() {
  return (
    <LogIn />
  )
}

export default Login